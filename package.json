{
  "name": "npm-workspaces-test",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "apps/elephant",
    "apps/giraffe",
    "packages/client",
    "packages/server"
  ]
}
